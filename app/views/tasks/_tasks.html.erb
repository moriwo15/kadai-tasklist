<ul>
  <% tasks.each do |task| %>
    <li>
      <div>
        <%= link_to task.user.name, user_path(task.user) %>
        <span>posted at<%= task.created_at %></span>
      </div>
      <div><p><%= task.content %></p></div>
    </li>
  <% end %>
  
  
</ul>